import { UpdateCustomerInput } from './update-customer.input';

describe('UpdateCustomerInput', () => {
  it('should be defined', () => {
    expect(new UpdateCustomerInput()).toBeDefined();
  });
});
