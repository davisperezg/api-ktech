import { AuthChangePasswordInput } from './auth-change-password.input';

describe('AuthChangePasswordInput', () => {
  it('should be defined', () => {
    expect(new AuthChangePasswordInput()).toBeDefined();
  });
});
