import { UserRefreshTokenType } from './user-refresh-token.type';

describe('UserRefreshTokenType', () => {
  it('should be defined', () => {
    expect(new UserRefreshTokenType()).toBeDefined();
  });
});
