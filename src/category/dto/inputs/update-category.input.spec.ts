import { UpdateCategoryInput } from './update-category.input';

describe('UpdateCategoryInput', () => {
  it('should be defined', () => {
    expect(new UpdateCategoryInput()).toBeDefined();
  });
});
